/* The system will be enabled after 500 servers reached */

/* Code: */
/*
import { ShardingManager } from "discord.js";

const manager = new ShardingManager(``, {
totalShards: 1,
token: process.env.TOKEN
});

manager.on("shardCreate", shard => console.log(`Shard ${shard.id} conectada.`))

manager.spawn()
*/